<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/ItemImageViewDisplay"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp">

    <ImageView
        android:id="@+id/PhotoOrdonnance"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="#EAFFED"
        android:contentDescription="@string/imageordonnance"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintDimensionRatio="H,1:1"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.0"
        tools:src="@drawable/ic_person_48" />

    <ImageView
        android:id="@+id/DeleteBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="@string/deleteicon"
        android:visibility="invisible"
        app:layout_constraintBottom_toBottomOf="@+id/PhotoOrdonnance"
        app:layout_constraintEnd_toEndOf="@+id/PhotoOrdonnance"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@+id/PhotoOrdonnance"
        app:srcCompat="@drawable/ic_baseline_delete_50" />

</androidx.constraintlayout.widget.ConstraintLayout>